import { describe, it, expect } from "vitest";

describe("Stadium Knowledge Network App", () => {

  it("should start successfully", () => {

    const appStatus = true;

    expect(appStatus).toBe(true);

  });

});