import { describe, it, expect } from "vitest";

describe("Stadium Components", () => {

  it("should render stadium component successfully", () => {

    const componentLoaded = true;

    expect(componentLoaded).toBe(true);

  });

});